#!/bin/sh
# Basically copied from plan9port's E script.
path=`realpath "$1"`
if ! [ -e "$path" ]; then
	: >"$path" || exit 1
fi
stat=`ls -l "$path"`
edit "$@"
echo editing "$path"
while sleep 1; do
	nstat=`ls -l "$path"`
	if [ "x$stat" != "x$nstat" ]; then
		exit
	fi
done
